# Logging parameters
folder: checkpoints/

# LeJEPA parameters
lambda: 0.03

# AdamW parameters
weight_decay: 0.01
lr: 3e-4

# Model parameters
model: resnet18
embedding_dim: 256

# Training parameters
device: cuda
dtype: bfloat16
epochs: 200
batch_size: 64
num_workers: 16
global_views: 3
local_views: 8